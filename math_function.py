import math 
import math as m # here m is alias
from math import sqrt


c= print(math.pow(5,2))
d= print(sqrt(4))
# 1. sqrt 2. pow 3. floor ( 2.9 = 2) 4. ceil ( 2.9= 3)
# 

print ( m.pi)

'''from math import sqrt: This form of import allows you to directly import a specific function, in this case, the sqrt function from the math module. By using this syntax, you can use the sqrt function directly in your code without explicitly referencing the module name.

Example:


from math import sqrt

result = sqrt(16)
Advantage:

It provides a more concise syntax, as you can directly use the function name without referencing the module name.
import math: This form of import imports the entire math module into your code. To use the functions from the math module, you need to prefix them with the module name.

'''

'''
from math import sqrt: This form of import allows you to directly import a specific function, in this case, the sqrt function from the math module. By using this syntax, you can use the sqrt function directly in your code without explicitly referencing the module name.

Example:


from math import sqrt

result = sqrt(16)
Advantage:

It provides a more concise syntax, as you can directly use the function name without referencing the module name.
import math: This form of import imports the entire math module into your code. To use the functions from the math module, you need to prefix them with the module name.

'''